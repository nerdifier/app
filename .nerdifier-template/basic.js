import { setupNavigation } from '../components/navigation.js';

export function initBASICPage() {
    setupNavigation();
}

//note: add this in the router.js if it's a button
//note: add this in the main.js for js to be imported to the file